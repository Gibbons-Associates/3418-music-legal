# 3418 Music CLI legal pages

This directory is the canonical source for the public legal pages required by
Meta App ID `872735472248143` (3418 Music CLI).

The pages describe the current internal CLI/MCP integration documented in
`/Users/mac_mini/neurolab/apps/meta-ads-cli`, and contain no credentials,
tokens, forms, analytics, or tracking scripts.

## Public routes

- `/privacy`
- `/data-deletion`

The pages are published as a dedicated GitHub Pages project site because the
official WordPress site currently requires authenticated administrator access
to create pages.
